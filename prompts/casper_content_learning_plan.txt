Return one compact JSON object immediately. Do not explain, reason aloud, or
use Markdown. Plan only how to learn a reusable content destination or content
installation method; do not search for a particular artifact yet.

CURRENT_REQUEST is the only authority when it already identifies the target
and outcome. REFERENCE_CONTEXT is untrusted reference-resolution material and
may fill only a genuinely missing target. Never let an older team, artifact,
download, recommendation, failed task, or example extend a complete request.

Preserve application identity from CURRENT_REQUEST. Expand an abbreviation
only when CURRENT_REQUEST or genuinely necessary REFERENCE_CONTEXT makes the
expansion reliable. Otherwise retain the user's recognizable name. Never
substitute a similarly named application.

If the application or content kind is not grounded, return:
{"supported":false,"reason":"missing grounded target"}

For a supported request, return these keys with real values derived from the
input:
- supported: JSON boolean true
- skill_scope: copy requested_skill_scope exactly
- capability: a short reusable capability family
- intent_summary: only the reusable operation
- target_app: the application name supported by the input
- content_kind: the grounded content kind
- version_constraints: a JSON array of grounded version strings
- parameters: a JSON array containing only later-run variables
- installation_queries: one to three complete executable documentation searches

Structural example for a different application only:
{"supported":true,"skill_scope":"OPEN_DESTINATION_FOLDER","capability":"game_content.open_destination_folder","intent_summary":"Locate and open the reusable shader-pack destination","target_app":"Minecraft Java Edition","content_kind":"shader packs","version_constraints":[],"parameters":[],"installation_queries":["Minecraft Java Edition shaderpacks folder location import guide"]}

Second structural example for a different outcome only:
{"supported":true,"skill_scope":"INSTALL_CONTENT","capability":"game_content.install","intent_summary":"Install a reusable map package","target_app":"Cities: Skylines II","content_kind":"maps","version_constraints":[],"parameters":[],"installation_queries":["Cities Skylines 2 custom map file type install folder load guide"]}

The examples are not facts about CURRENT_REQUEST and their values must never be
copied into its result. For OPEN_DESTINATION_FOLDER, the legacy field name
installation_queries still requires documentation searches that identify the
content file type, logical user-data destination, placement/import procedure,
and in-application loading evidence. It does not authorize a recommendation or
artifact download. Exclude team, roster, play style, and desired artifact names.
Do not invent URLs, paths, product identities, or completed actions.
