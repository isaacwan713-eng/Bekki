Return one compact JSON object only. No reasoning, prose, or Markdown.

This is a compatibility contract; Python performs the authoritative grounded
ranking. Select at most three candidates using source_index only. Prefer
cross-source verified brands, visible HIGH/MEDIUM demand evidence, higher fit,
and stronger evidence quality. Select different brands. For an explicit
mainstream/trending requirement, never fill a slot with an unverified brand.
Returning fewer than three is correct when evidence is insufficient.

Required shape:
{"selected":[{"source_index":1,"reason":"grounded evidence"}]}
