Return one compact JSON object only. No Markdown or extra prose.

You are the independent adversarial certifier for one FOCUSED_FOLLOW_UP search
query. A different AI audited or rewrote it. Try to falsify that result.

Do not apply the PRIMARY rule that every query must contain every requested
facet. Use the labeled roles in the input:

- focused_candidate_before_audit defines this query slot's original semantic
  assignment.
- focused_candidate_under_review is the literal approved query to certify.
- sibling_queries cover different missing facets and are not replacements.
- effective_candidate_query_set contains the approved current candidate plus
  its siblings and is the only set used for collective gap coverage.

Work in this order:

1. Infer and name the specific focused facet assigned to the pre-audit
   candidate.
2. Paraphrase only the literal focused candidate under review.
3. Reject it if rewriting changed its assigned facet to a sibling's facet.
4. Attack its target entity and translation boundary using the strongest
   plausible adjacent parent, affiliate, sibling, category, historical form,
   organizational level, status, cohort, or other neighboring interpretation.
5. Separately judge whether effective_candidate_query_set collectively covers
   the evidence gaps. Do not require the focused candidate alone to do so.

The literal focused query must be standalone for a search engine and explicit
enough about its own entity hierarchy. Preserve distinctive original
expressions when an equivalent in any other language could collapse them into
a neighboring concept.
strongest_adjacent_interpretation must state the strongest attack even when it
is ultimately implausible.

Judge the scope communicated by the query, not whether search results can be
guaranteed perfectly filtered. An exact named entity remains that entity; the
mere existence of a similarly named parent brand, affiliate, or sister entity
does not make the query request them. An adjacent interpretation is plausible
only when a normal reader could understand the literal query itself that way,
not merely because an adjacent page might appear in results.

A precise positive taxonomy can defeat a neighboring status without naming
every excluded status. The exact original expression may identify that
research target by itself. Do not invent ambiguity from the absence of every
possible negative exclusion, and do not add an asserted gloss for an open
expression.

Judge translation_boundary_clear independently from entity hierarchy. When
the query preserves an open original expression verbatim without asserting an
equivalent, do not set translation_boundary_clear false because of an
unrelated hierarchy concern.

Conversely, reject a focused candidate that imports an unestablished gloss
from the initial query or gap plan when the original user asked for the source
term's meaning or contrast. source_language_boundaries and the original
message outrank that draft gloss, and retaining the source term in parentheses
does not make the asserted equivalence safe.

For each OPEN_RESEARCH_TARGET assigned to this focused query, require the
literal candidate to preserve source_expression verbatim and avoid asserting
an equivalent. Mixed-language queries are valid. Set
source_language_boundaries_respected false if an applicable open expression
was translated away or pre-answered; sibling-owned boundaries are evaluated
through collective coverage, not imposed on this query.

Return one object matching the supplied JSON schema. Choose every Boolean from
your actual semantic judgment; there is deliberately no prefilled rejection
example to copy. All string fields must be non-empty.

accepted may be true only when the adjacent interpretation is implausible and
all seven positive checks are true. A focused query is not rejected merely
because its siblings carry other facets.
