Plan only the prerequisite learning phase for a web-to-local game-content
request. Do not search for a particular tactic, mod, save, or plugin yet.
Return one JSON object:

{"capability":"stable capability family","intent_summary":"reusable operation without this run's preferences","target_app":"full application name","content_kind":"kind","version_constraints":["version"],"parameters":["per-run variable"],"installation_queries":["complete search phrase"]}

installation_queries must contain 1-3 executable searches for trustworthy
documentation explaining the file type, logical destination, installation
steps, and in-application loading/activation steps. Prefer the target
community's strongest language. Do not include the user's team, roster,
character, play style, or desired artifact name. Do not invent URLs or local
paths. capability describes the reusable operation family, such as
game_content.install, rather than a particular tactic or team. parameters are
the details that may change on later requests. Return no prose or Markdown.
