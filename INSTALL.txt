Bekki Screenshot Multipass OCR V1.10.27

Recommended installation:

1. Close Bekki and its knowledge scheduler.
2. Extract the ZIP outside your current AI-Assistant folder.
3. Run INSTALL_STABLE_V1.bat.
4. The default target is %USERPROFILE%\AI-Assistant.
5. Download the required local models:
   ollama pull gemma4:12b
   ollama pull gemma4:e4b
   ollama pull llama3.2:latest
6. Start Bekki from that target with: python main.py

The installer preserves your existing `.env`, `data`, `.git`, `.venv`,
`build`, and `dist`, creates a timestamped backup, validates the new Python
files, and restores the prior runtime automatically if installation fails.

Expected startup build ID: bekki-screenshot-multipass-ocr-v1-10-27-20260830

Screenshot Multipass OCR V1.10.27 reads a short panoramic UI screenshot in
three local Windows OCR views: the full image and overlapping enlarged left
and right details. The existing Gemma Vision call compares those independent
transcriptions with its image tiles; no new AI role or gate is added. Up to 24
literal UI strings now survive the evidence packet instead of 12, raw local
OCR observations remain available to the final image answer, and uncertain
characters or unlabeled metrics must stay uncertain rather than becoming a
fluent guess.

Screenshot Current Evidence V1.10.26 keeps an attached image as the
authoritative current-turn evidence through final answer generation. An image
explanation remains TASK + IMAGE even if the router also emits COMPANION, so
older screenshots and prior assistant answers are not loaded and cannot
replace the current image. It also updates the local Windows OCR WinRT awaiter
and stream type and reports the exact failure stage, exception type, and
HRESULT if OCR is unavailable. No AI role or semantic routing gate was added.

Screenshot OCR Fusion V1.10.25 adds local Windows.Media.Ocr transcription and
overlapping enlarged screenshot tiles to the same existing Gemma vision call.
It does not add another AI role or routing gate. Chinese and other source text
must remain in the original writing system, while unresolved OCR/image
conflicts are reported as uncertainty rather than guessed. If Windows OCR is
unavailable, Bekki safely continues with the enlarged Gemma tiles. For the
90% clear-UI key-field acceptance target on Simplified Chinese screenshots,
Windows should have the zh-CN OCR recognizer installed.

Screenshot Local Route Contract V1.10.24 fixes a screenshot-only LOCAL route
being rejected when MAGI used `local_knowledge_sufficiency=SUFFICIENT` to
describe the attached image even though the recalled Knowledge packet was
empty. The runtime now normalizes only that mechanically impossible auxiliary
value to NONE and keeps the valid AI-selected lane, avoiding an unnecessary
recovery-model call. No new AI role or routing gate was added.

Completed Historical Milestone Knowledge V1.10.23.2 makes the existing time
scope and lifecycle AIs treat a request to discover a public debut, founding,
first release, first appointment, or similar completed milestone as
EXPLICIT_PERIOD rather than current state. When accepted evidence supports the
exact date or year, its atomic claim may enter Knowledge as FIXED_HISTORY.
Retrieval freshness metadata cannot override the completed historical meaning.
No new AI role or synchronous answer gate was added.

Curiosity Foundation Draft Calibration V1.10.23.1 strengthens the existing
Writer without adding another AI role or retry gate. A safe FOUNDATION turn
about an identified public subject now drafts a distinct approachable question
when an uncovered facet remains. A narrow history of units, terminology, and a
dated roster no longer becomes proposal=null merely because the packet does
not already contain a named performance, person, or story. The question may
ask for such an example without asserting an unsupported answer.

Curiosity Native Knowledge and Idle Exploration V1.10.23 lets the existing
Curiosity Writer continue from newly verified Knowledge at later idle checks.
The next draft retains exact source Curiosity/Knowledge IDs and must still pass
the existing privacy, duplicate, breadth, and gradual-depth contracts.
Unverified or transient current-state answers cannot seed further exploration.
The legacy daily default of three is migrated to ten for this test cycle.

Curiosity-verified durable explanations, reviewable maintained structures, and
exact closed historical snapshots now enter the same Knowledge inbox used by
ordinary audited FACT_LOOKUP. Current rosters, affiliations, schedules, events,
and news remain journal-only. A new item created after today's Curator run is
eligible for another same-day idle curation pass.

Daily curation now stores a presentation-only preferred name form: established
Chinese public names normally use Chinese, established Japanese public names
use Japanese, and original English or romanized spellings remain aliases. An
uncertain conversion remains source-preserved. The authoritative verified
claim is never rewritten. No new AI role or synchronous reply gate was added.

Historical Snapshot Lifecycle Calibration V1.10.22.1 corrects the existing
partition/lifecycle contract so an exact completed historical people roster is
FIXED_HISTORY rather than a current roster. The first AI's persist proposal is
advisory; the existing lifecycle auditor can correct a false changing decision
when claim text and temporal metadata preserve the same source-supported closed
snapshot. Current and open-ended rosters still never enter Knowledge. No AI
role or model call was added.

Knowledge Intake V1.10.22 processes ordinary low-risk FACT_LOOKUP answers only
after Casper has accepted them and after the user reply has been returned. The
existing partition and lifecycle AI roles decide which exact claims can enter
Knowledge. A source-supported closed historical roster snapshot may be stored
with its exact period; a current people roster or current affiliation is never
stored. The background result is visible as `[NERV FACT KNOWLEDGE INTAKE]`.

Curiosity Breadth V1.10.22 requires a new or developing topic to move from a
foundation list or roster to a genuinely different approachable facet. Member
roles, newest members, and another status slice remain the same narrow roster
lane. The existing Writer and Selector own this semantic decision; no new AI
gate or Python domain classifier was added.

Curiosity Foundation Calibration V1.10.21 makes topic maturity depend on
distinct, learned foundation coverage instead of the raw number of related
records. Drafted, asked, or unverified Curiosity cannot by itself make a topic
mature. A FOUNDATION turn in a NEW_OR_SPARSE or DEVELOPING topic must produce
a concrete FOUNDATION proposal. A comparison inherits its hardest requested
knowledge lens, so brand positioning, audience segmentation, promotion
strategy, contracts, governance, and professional operations remain
SPECIALIST even when phrased as differences. The existing Writer and Selector
own these semantic decisions; no new AI model call or Python keyword
classifier was added.

Knowledge Breadth V1.10.20 strengthens the existing Curiosity Writer rather
than adding another AI gate. For a new or sparse topic, Bekki first asks an
approachable foundation question about people, history, works, events,
relationships, stories, culture, or ordinary behavior. Specialist contracts,
governance, strategy, or methodology follow only when the user's current turn
is already specialist or the supplied related history supports that depth.
The Writer receives bounded active Knowledge plus recent curiosity IDs and
must declare its topic stage and question depth; Python only validates that
declared contract.

Historical Snapshot Knowledge V1.10.20 distinguishes a current people roster
from a completed historical roster. An active/current roster remains SEARCH
and never enters Knowledge. A roster tied to an exact completed date or closed
season may enter as FIXED_HISTORY only when its exact period metadata is
preserved. Different dates or seasons receive distinct knowledge identities,
are never deduplicated merely for sharing an entity, and can answer only the
same historical period. The existing MAGI call owns the sufficiency decision;
no new model call was added.

Knowledge-Aware Routing V1.10.19 moves deterministic active-Knowledge recall
before MAGI. The existing MAGI call decides whether the recalled claims fully
cover the exact request. A complete match routes LOCAL and bypasses redundant
Melchior routing; incomplete, mismatched, expired, or explicitly reverified
requests still route SEARCH. The same candidates reach the final answer, and
no new AI gate or model call was added.

Adjacent Curiosity V1.10.19 makes first encounters stay close to the user's
topic and level before advancing to specialist mechanisms. The existing
Curiosity Writer receives bounded prior-question history; Python does not
select topics or semantic depth.

Audit Finding Normalization V1.10.18 keeps the existing Answer Auditor as one
model call but removes its duplicate global verdict. The model now returns
semantic findings plus a mandatory canonical answer; the runtime maps those
findings consistently without deciding factual content. A removable false
event or justification is deleted while a complete, non-conflicting core is
returned and stored. No new AI gate or model call was added.

Core Claim Isolation V1.10.17 makes the existing Answer Auditor evaluate a
complete retained core independently from removable false additions. Latent
model familiarity and missing web corroboration are not negative evidence;
only a direct contradiction affecting the retained core or supplied bounded
web conflict evidence may reject it. No new AI gate or model call was added.

Audited Core Answer V1.10.16 lets the existing Answer Auditor remove
unnecessary or unreliable adjacent assertions when the remaining core still
fully answers the exact question. The cleaned core—not the noisy original—is
returned and stored. The auditor also receives Bekki's host-local current date
as the authoritative temporal reference. No new AI gate or model call was
added, and Python does not decide which facts to keep.

Answer Audit Scope Anchor V1.10.15 keeps the exact user question and the
External-AI candidate answer as the binding final-audit pair. Web material is
limited to six compact conflict excerpts and cannot redefine the request,
completion target, or entity relationship. The exact request is repeated at
the end of the audit packet, while page bodies and large prior certification
text are excluded. This prevents an adjacent member-list page from turning a
formal-Team question into a request for every member. No AI role, gate,
arbiter, external request, keyword classifier, or domain-specific rule was
added.

Nonstructural Lifecycle Boundary V1.10.14 gives the standalone fallback's
transient branch an explicitly nonstructural meaning. Current people, rosters,
schedules, prices, scores, versions, availability, and events remain
current-turn-only; a current complete list of formal units remains maintained
reusable structure. Browser-generated entity facets no longer broaden the
authoritative question during lifecycle judgment, and the existing second AI
audits lifecycle without seeing the first AI's lifecycle conclusion. No AI
role, gate, arbiter, keyword classifier, or model call was added.

Fallback Lifecycle Basis V1.10.13 makes standalone External-AI fallback
questions choose an explicit semantic basis before a lifecycle label. A
present list of formal organizational units remains reusable maintained
structure even when the request says `current` or `目前`; current people,
individual affiliations, schedules, prices, scores, versions, and events stay
current-turn-only. This strengthens the existing Governor and independent
policy auditor without adding another AI call. Python checks contract
consistency but does not classify subjects or keywords.

Structure Lifecycle Authority V1.10.12 distinguishes a maintained list of
formal organizational units from a roster of people. The existing lifecycle
AI may correct the first partitioner's persistence proposal without adding a
new model call. Python permits storage only for low-impact, directly supported,
uncontradicted, sufficiently confident claims. Current people and individual
affiliations remain current-turn-only. Lifecycle audit V7 rechecks older mixed
claims without silently rewriting them.

Language-Safe Knowledge Review V1.10.11 preserves any meaningful expression
that has no safe exact equivalent. Ordinary fact lookup may use mixed-language
queries and still completes bounded 3-5-7 research. Social search keeps all
meaningful user search terms in their original form on every platform.

Maintained structures may be stable or reviewable according to one lifecycle
AI. Stable knowledge has no fixed expiry but is eligible for a randomized idle
review. A supported or inconclusive review never rewrites the claim; strong
contradiction quarantines it as disputed. The obsolete final lifecycle critic
is not used.

Semantic Grounding V1.10.10 strengthens existing AI contracts without adding a
query arbiter, AI role, gate, or normal-path model call. When a source-language
expression has no reliable exact translation—or its meaning is itself being
researched—the query keeps that original expression verbatim and may mix
languages. Lifecycle V5 requires the existing AI to select a semantic basis
before its label, preventing a durable-definition explanation from being
stored as reviewable or a maintained official set from becoming permanent.
Daily curation records the literal claim subject, selected and rejected
hierarchy IDs, and relationship evidence while keeping related entities in one
ecosystem JSON without merging them.

Focused Certifier Calibration V1.10.8 strengthens the existing focused audit
and certification prompts only. It adds no AI role, gate, arbiter, or model
call. A precise positive entity and source taxonomy can defeat a neighboring
interpretation without listing every excluded status. The certifier judges the
literal query's meaning rather than possible irrelevant search results, and
focused prompts no longer contain prefilled rejection-biased JSON examples.

Query Certifier Calibration V1.10.7 keeps the existing two-role query flow:
audit, certify, and at most one retry through those same roles. It adds no
third AI, arbiter, or model call. The certifier now evaluates literal search
scope rather than requiring impossible guarantees about every returned page.
Explicit positive identity and adjacent-scope exclusion wording can establish
the hierarchy boundary. Fixed rejection-biased JSON examples were removed,
and the original auditor's retry instructions were strengthened.

Focused Query Set V1.10.6 gives focused evidence-gap queries their own AI
audit and certification contracts. The current candidate, its pre-audit facet,
and sibling queries are labeled separately. AI must preserve the current
candidate's facet through rewrites while judging full gap coverage across the
effective query set. It no longer applies the PRIMARY requirement that every
single focused query contain every requested facet. Python validates only the
structured AI decisions and does not classify entity or taxonomy terms.

Recycle Action Arbiter V1.10.5 fixes stale old-chat influence inside Casper's
Recycle Bin executor. The broad action planner and focused restore-intent AI
judge independently. A disagreement is resolved by a third AI whose input
places the current request before old reference context. If arbitration fails,
Bekki asks what the user means and never enters item restoration. Python
validates only the closed AI action tokens; it contains no language keyword
rule for open, list, or restore.

Knowledge Correction V1.10.4 retains `gemma4:12b` for
primary answers, MAGI, research, vision, learning, verification, and final
writing, plus `gemma4:e4b` for fast routing and audits. The old Gemma 3 weights
may remain installed for rollback but are not loaded by this build.

Gemma 4 uses a different prompt and thinking contract. V1.10.4 sends reusable
instructions through Ollama's native system field and converts thinking to a
strict Boolean. Existing quick/JSON calls remain thinking-disabled; only an
explicit high/on/True request enables it. The 12B context remains capped at 8K
and E4B at 4K for the 16GB GPU stability envelope.

V1.10.4 adds user-triggered Knowledge correction. A local AI decides whether a
correction concerns a user-authoritative personal fact or an externally
checkable public fact. Public disputes temporarily deactivate only the exact
AI-selected records, re-run FACT_LOOKUP without treating the user as evidence,
and preserve the old claim in `revision_history`. A verified reusable answer
refreshes or supersedes the record; an inconclusive result remains inactive.

Mixed-answer lifecycle V3 adds a final independent durability critic whenever
any claim would be permanent `stable`. Maintained official unit sets become
periodically `reviewable`; durable definitions and mechanisms may remain
`stable`; current affiliations, rosters, and schedules stay current-turn-only.
V2 mixed records are automatically re-audited during idle curation.

Objective Fact Verification V1.8 audits an ungrounded LOCAL draft before it is
shown when the answer contains concrete public-person, organization, team,
roster, status, version, or date claims. A VERIFY verdict automatically reroutes
the original request through official-first FACT_LOOKUP. If reliable evidence
cannot be obtained, Bekki withholds the draft instead of guessing. Greetings,
creative writing, translation, math, and user-authoritative facts about the
user, family, devices, and preferences retain the fast local path.

After bounded 3-5-7 FACT_LOOKUP fails to produce a complete answer, Bekki can
ask ChatGPT Desktop with the exact public question. Permanent stable facts are
stored after an answer-scope audit. Reviewable structural facts are stored with
an AI-selected recheck deadline. Fast-changing person/team/status facts answer
only the current turn and are not stored. High-impact contract, legal, medical,
financial, safety, and reputational questions require a second certification
before display and are not automatically stored.

Approved Curiosity facts and low-impact stable/reviewable External-AI facts now
enter `data\knowledge\inbox.json`. At the first idle opportunity once per local
day, Gemma 4 organizes them into broad topic-ecosystem JSON documents under
`data\knowledge\topics`. Related organizations, members, and sister groups can
share one document while retaining separate entity IDs. Mixed questions are
partitioned by AI: reusable structure may enter Knowledge, while current
members, rosters, and schedules remain current-turn-only.

V1.10.4 curates the inbox in complete three-item batches and atomically commits
each successful batch. It also recognizes newly-created ChatGPT Copy controls
by UIA runtime identity, so a WebView that replaces one old control with one
new control cannot hide a completed answer merely because the count stayed the
same. Timeout logs now state whether the exact prompt, a new Copy control, or a
still-generating response was observed.

Before browser discovery, local AI now derives the exact entity hierarchy and
requested facets from the original message. A separate AI audits and may
rewrite the initial and follow-up search queries. Another independent AI
adversarially interprets the literal query without hidden runtime assumptions.
It must construct the strongest excluded-scope reading; a plausible translated
hierarchy ambiguity is repaired once and challenged again. Distinctive
source-language taxonomy is preserved when an exact translation is uncertain,
and focused follow-up queries are judged collectively rather than each being
forced to repeat every requested facet. A separate
independent lifecycle auditor also rechecks whether External-AI results should
be stable, periodically reviewable, or current-turn-only and chooses any
review interval semantically rather than from a fixed day table. Another AI
rejects final evidence that answers for a nearby parent, affiliate, sibling,
category, or other different entity. Missing or wrong-scope evidence is not
proof that a fact is unavailable; it proceeds to the governed External-AI
fallback. These are semantic AI decisions, not domain-specific Python keyword
rules.

Every reusable claim separated from a mixed current answer requires an
independent local-AI lifecycle judgment before it can be written. The auditor
distinguishes durable explanations from periodically reviewable active
structures and complete official sets. If that decision is non-proportional or
disagrees with the first AI's reusable lifecycle, a third independent AI
arbitrates. Any resulting permanent claim then receives the separate V3
durability critique, including cases where earlier judges agreed. Existing
older-audit mixed claims, including records wrongly
demoted to current-only, are automatically re-audited during idle curation;
corrected records are requeued without clearing Knowledge or other user data.

Curiosity now receives no raw LOCAL reply text. This prevents a hallucinated
team, person, date, or other proper noun from being copied into an External AI
question. Evidence-backed FACT_LOOKUP and CLAIM_CHECK replies may still inform
Curiosity.

NERV Curiosity Soft Gate V1.7 treats the writer's interest and confidence
scores as uncalibrated selector hints instead of normal hard rejection gates.
Only extremely low confidence below 0.40 is rejected before selection. Privacy,
language, malformed-contract, and duplicate protections remain enforced, and
dismissal logs now include the exact reason.

External AI Answer Anchor V1.6 reads only the ChatGPT Desktop response that
follows the exact prompt sent in the current request. It snapshots off-screen
UIA text before sending, scrolls the newly-created response control into view,
and rejects old answers revealed by viewport movement.

Knowledge Clusters V1.5 lets Curiosity investigate stable background knowledge
and useful current snapshots while enforcing a stable-only Knowledge boundary.
Current rosters, trades, injuries, prices, schedules, policies, software
versions, events, and news are never promoted into long-term Knowledge. Basic
stable facts use external-AI corroboration plus one qualified independent
source. Medical, legal, and sufficiently technical claims require two sources,
consensus, and a second certification pass. Verified facts are clustered in
`data/knowledge_clusters.json` and can be recalled locally for later related
questions without a new search.

News Feed Reliability V1.4 keeps relative recent-month searches inside a
bounded current window, retries malformed structured extraction once, and
fails closed instead of turning an extraction error into "no news".

NERV Context Isolation V1.3 prevents a self-contained current message from
receiving an older topic during Melchior routing or MAGI lane recovery. Prior
conversation is exposed only for explicit follow-ups such as "那这个是真的吗".

This update lets an explicitly attached screenshot ground MAGI and Melchior
before Casper searches. Local Vision extracts exact visible names, model
numbers, sports entities, claims, and error codes once; only privacy-filtered
text can reach the search pipeline, and the screenshot itself is not uploaded
to a search engine. Image explanation remains local, while requested claim
verification, fact lookup, news, or shopping research reuses Casper's existing
evidence checks. It retains the top-right appearance settings button, saved
fonts and custom avatar, and the installed ChatGPT
Desktop integration instead of browser/CDP control, and supports
up to ten privacy-screened NERV Curiosity Journal questions per local day. Existing
data, memory, environment settings, and verified Casper Skills are preserved.
The installer adds pywinauto to the existing virtual environment when needed.
Install and sign in to ChatGPT Desktop before the first live test. No web
fallback is used. See EXTERNAL_AI_DESKTOP_V1_NOTES.md for the boundaries.

V1.1 waits for the desktop WebView accessibility tree to finish loading. If
the main composer remains unavailable, it opens ChatGPT's official Companion
Window with Alt+Space and continues only after observing a distinct ChatGPT
window. It still uses no browser or screen-coordinate clicking.

V1.2 rejects ChatGPT's non-editable "Composer utility bar" so it cannot be
mistaken for the message box. That condition now reaches the safe Companion
Window fallback.

V1.3 adds direct Windows Unicode keyboard input when the editable ChatGPT
control is found but the Windows clipboard is temporarily unavailable. This
supports Chinese prompts without using browser or screen-coordinate clicking.

V1.3.1 keeps the verified V1.3 foreground send, minimizes ChatGPT immediately
after submission, and reads the answer through background UI Automation. It
fixes 64-bit clipboard handles, rejects `You said:`/404 UI text as answers, and
keeps Chinese questions Chinese unless translation was explicitly requested.

V1.3.2 keeps ChatGPT open behind Bekki while waiting, because the live Desktop
WebView stops exposing new answer nodes when minimized. Bekki regains focus
immediately after submission; ChatGPT is minimized after answer capture.

NERV Knowledge Verification V1.4 treats every ChatGPT answer as an unverified
hypothesis. A low-risk, durable factual candidate must pass an independent
3→5→7 search and receive consensus from at least two readable high-quality
domains before it can enter Knowledge. Otherwise it remains unverified.
For why/cause/mechanism questions, the candidate must contain the central
explanation. A side fact triggers one bounded recovery attempt and cannot be
promoted merely because it is easy to verify.

Legacy R25 development notes follow below.

Bekki V2 - Casper Device Action Phase 13.0

Copy every folder/file in this package into the root of your current
AI-Assistant project and allow Windows to replace files with the same names.

Changed files:
- melchior.py
- casper/adapters.py
- casper/safety.py
- prompts/melchior_router.txt
- prompts/melchior_router_recover.txt

New files:
- casper/device_actions.py
- prompts/casper_device_action.txt
- tests/test_device_actions.py

Validation:
python -m py_compile melchior.py casper\core.py casper\adapters.py casper\safety.py casper\device_actions.py
python -m unittest -v tests.test_device_actions
python main.py

Manual tests:
- 打开 Steam
- 打开 Discord
- 打开一个不存在的应用
- 打开 Steam 里的游戏

Phase 2 also discovers installed Steam games from local app manifests. Test:
- 打开 Football Manager 26

Phase 3 adds bounded local-library queries. Test:
- 我的 Steam 库里有什么已安装游戏？
- 我电脑音乐库里有哪些歌？
- 我的 Spotify 收藏里有什么歌？ (must report unsupported until connected)

Phase 3.1 safely recovers an OPEN_APP selection when the local model truncates
JSON after already returning a valid discovered candidate ID. Test:
- Ask for the Steam library, then say 打开第二个

Phase 4 resolves supported launcher shortcuts. It reports launcher_opened when
only a launcher appears and opened only when the discovered game binary or
requested game window is observed. Test:
- 帮我打开原神

Phase 5 adds AI post-launch verification from bounded process/window evidence.
The final result is one of GAME_OPENED, APP_OPENED, LAUNCHER_OPENED, FAILED,
or UNCERTAIN. New file:
- prompts/casper_launch_verify.txt

Phase 6 adds supervised Windows elevation. On WinError 740, Bekki pauses and
asks for chat approval. Reply 继续 to request the native UAC dialog, then click
Yes personally. Casper verifies the observed launch state afterward.

Phase 6.1 fixes truncated pending-action confirmation. Confirmation remains an
AI judgment; invalid output triggers one smaller AI retry and never a Python
keyword decision. Added test file:
- tests/test_confirmation.py

Phase 7 adds a launcher-agnostic Launcher -> Game contract layer. Casper asks
the installed launcher to start its game, then AI judges bounded post-launch
process/window evidence. HoYoPlay/Genshin Impact is the first adapter and uses
launcher.exe --game=hk4e_global, including when HoYoPlay is already running.
The normal launcher route should not request elevation. Direct game-executable
launching is only a bounded fallback and may still require chat approval/UAC.

Phase 7 validation:
python -m unittest -v tests.test_confirmation tests.test_device_actions

Expected manual result for 帮我打开原神:
- GAME_OPENED when GenshinImpact.exe or its game window is observed.
- LAUNCHER_OPENED when only HoYoPlay is observed.
- No success claim merely because the launcher was already open.

Phase 8 adds supervised launcher interaction. If the first verification finds
only a launcher, Casper reads a bounded Windows accessibility-control catalog.
AI may select exactly one opaque control ID for a clear Play/Start action;
Python invokes only that discovered control and then verifies the game again.
No model-generated command, path, keyboard input, or screen coordinate runs.

New file:
- prompts/casper_launcher_control.txt

Expected Phase 8 flow:
1. Bring the installed launcher forward using its contract.
2. If only the launcher is visible, AI selects its exposed Start/Play control.
3. Python invokes that bounded control.
4. AI confirms GAME_OPENED only when the target game process/window appears.

Phase 8.1 fixes device-planner output truncation inside candidate_id. Python
does not infer the intended application. It detects only the incomplete
transport envelope, then asks AI again with a compact prompt that may return
one exact opaque catalog ID or NONE. Unknown and invented IDs remain blocked.

New file:
- prompts/casper_device_action_retry.txt

Phase 8.2 fixes truncation in AI post-launch verification. If the full JSON
verdict is invalid, AI receives a compact retry and returns exactly one bounded
status token. A recovered LAUNCHER_OPENED result continues into Phase 8's
launcher-control discovery instead of stopping as UNCERTAIN.

New file:
- prompts/casper_launch_verify_retry.txt

Phase 8.3 moves Casper's compact bounded retry decisions to
llama3.2:latest: application-ID retry, five-state launch retry, and launcher
control selection. These remain AI judgments, while avoiding long internal
reasoning exhausting the output budget before the required token is emitted.

Phase 9 adds bounded visual launcher interaction when a launcher exposes zero
Windows accessibility controls. Casper captures only the already identified
launcher window. Local Vision may return CLICK or NONE plus normalized
window-relative coordinates. Python rejects out-of-window/title-bar points,
clicks only inside that launcher window, and verifies the game process/window
again. Vision is explicitly forbidden from selecting purchases, accounts,
ads, settings, repair, destructive, ambiguous, or unreadable controls.

Additional changed file:
- vision.py

Expected Phase 9 log when UI Automation exposes no controls:
- launcher_interaction.method = vision
- launcher_interaction.visual_label = visible Start/Play label
- launcher_interaction.clicked = true
- final action = game_opened only after target game evidence appears

Phase 9.1 fixes visual clicks on 64-bit Windows by passing the discovered
window handle as an explicit pointer-sized HWND to Win32 validation, focus,
and bounds calls. It also retries an entirely empty device-planner transport
response through the compact AI selector, which fixes short contextual replies
such as 需要 after Bekki asks whether it should try the same game again.

New diagnostics:
- [CASPER VISUAL CLICK] clicked x y
- [CASPER VISUAL CLICK] rejected invalid launcher HWND
- [CASPER VISUAL CLICK] SetCursorPos failed x y
- launcher_interaction.visual_point

Phase 9.2 adds a Win32 SendInput absolute-coordinate fallback when
SetCursorPos is unavailable. Coordinates are converted through the Windows
virtual desktop metrics, including multi-monitor layouts, and remain bounded
to the authorized launcher window. LAUNCHER_OPENED and UNCERTAIN are now
incomplete results rather than successful game launches. The final reply is
explicitly forbidden from saying the game opened or wishing the user fun until
GAME_OPENED is verified.

Phase 9.3 prevents false GAME_OPENED reports. Casper now preserves a complete
AI-authored status field from otherwise truncated verifier JSON, so an emitted
LAUNCHER_OPENED continues into visual clicking instead of being guessed again.
As a mandatory execution invariant, GAME_OPENED is accepted only when the
bounded snapshot contains the expected game process or requested game window.
An unsupported AI success verdict becomes UNCERTAIN and can never produce a
completed/successful game launch response.

Phase 9.4 adds adaptive post-click verification. The initial launcher check
remains short. After Casper invokes or visually clicks a launch control, it
polls bounded Windows process/window evidence every three seconds for up to
about 35 seconds. It stops early as soon as the exact expected game executable
or requested game window appears, then asks AI for the final verdict. This
avoids declaring FAILED during a normal game startup delay.

Phase 9.5 runs the bounded launch-state classifier on llama3.2:latest from the
first attempt. If AI returns GAME_OPENED without mandatory target process or
window evidence, Python rejects only that unsupported success claim and asks
AI to reclassify among LAUNCHER_OPENED, FAILED, and UNCERTAIN. A visible
launcher can therefore continue into Phase 9 visual clicking instead of being
stopped as UNCERTAIN.

New file:
- prompts/casper_launch_verify_no_game.txt

Phase 10 fixes elevated launcher continuation. ShellExecuteW previously placed
the launcher's argument string in the hwnd slot and passed no lpParameters, so
accepting UAC opened HoYoPlay without the requested game contract. It now calls
ShellExecuteW(None, "runas", launcher_path, "--game=hk4e_global", ...).
UAC is still shown and must be approved by the user; after approval, the
elevated launcher receives the native game request instead of stopping at its
home window.

Phase 11 removes the HoYoPlay/Genshin-specific launch contract. After AI picks
an installed application, Casper resolves that shortcut locally and constructs
a bounded catalog containing only the shortcut's real target, its registered
arguments, nearby executable evidence, and visible window titles. A second AI
decision selects opaque IDs from that catalog. Python validates the IDs and
executes only the locally discovered values; it never invents or semantically
chooses a launcher command. The same flow therefore applies to HoYoPlay,
Steam, Epic, EA, Ubisoft, and future launchers without a per-game Python rule.

New file:
- prompts/casper_launch_strategy.txt

Phase 11 validation:
python -m unittest -v tests.test_confirmation tests.test_device_actions

Expected safety behavior:
- Shortcut arguments such as a game selector must come from the installed
  .lnk metadata, never from the prompt or a Python game-name table.
- AI may select only supplied strategy/executable/window IDs.
- Invented IDs fall back to the original signed shortcut and cannot execute.

Phase 11.1 fixes an empty launch-strategy response caused by the main local
model spending its output budget on reasoning. Strategy selection now uses a
compact llama3.2 decision and receives one compact retry if transport still
fails. It can see whether each supplied strategy carries registered shortcut
arguments, while Python continues to execute only the original local values.
If a launcher appears only after launch, a second bounded AI selection maps
one observed window ID to the launcher so accessibility or visual Play/Start
interaction can continue without a HoYoPlay-specific title rule.

Additional new files:
- prompts/casper_launch_strategy_retry.txt
- prompts/casper_launcher_window.txt

Phase 12 begins general internal Windows control. Melchior routes explicit
computer operations to DEVICE_ACTION; AI selects one bounded action and, for
window operations, one opaque currently discovered window ID. Python provides
and executes only this fixed catalog.

Supported Phase 12 controls:
- volume up/down/mute (one fixed step)
- media play/pause, next, previous
- brightness up/down (one fixed step when the monitor exposes Windows WMI)
- focus, minimize, maximize, or restore one discovered visible window

Not supported in this phase:
- arbitrary keyboard text/shortcuts or mouse coordinates
- exact numeric volume/brightness targets
- closing windows, shutdown/restart/sleep, shell commands, or file changes

Phase 12 validation:
python -m unittest -v tests.test_confirmation tests.test_device_actions

Manual tests:
- 音量调高一点
- 静音
- 暂停音乐
- 把 Edge 最小化
- 切换到 Steam 窗口

Phase 12.1 generalizes the compact device-planner retry. Previously the retry
could return only an application ID, so a truncated window/system-control plan
was misread as an application lookup. The compact AI can now return every
bounded Phase 12 action as structured JSON. If the user asks to switch to a
window that is not currently open, Bekki reports that fact and may ask whether
to open the application; it does not request the full name or silently launch
it. Truncated system and window actions are recovered only when their complete
AI-authored action and valid opaque target ID are present.

Phase 12.2 separates long-answer and control-plane token budgets:
- Bekki final replies: num_ctx=32768, num_predict=8192, low reasoning
- Melchior routing: llama3.2, num_ctx=8192, num_predict=512, no thinking
- Casper device planning: llama3.2, num_ctx=8192, num_predict=512, no thinking

This gives long conversations and synthesized answers more room without using
gpt-oss reasoning tokens for short JSON device decisions. On a 16 GB GPU, the
32K final context is intentionally the practical high setting; if sustained
CPU offload or severe latency appears, restore main.py to 16384/4096 while
keeping the llama3.2 control-plane changes.

Phase 12.3 fixes schema-example echo from the compact device model. A response
such as action="OPEN_APP | STEAM" is neither treated as a real action nor as a
semantic unsupported verdict. Python detects only that the action is outside
the fixed enum and asks AI for one compact structured retry. Execution occurs
only when AI then returns exactly OPEN_APP plus a real discovered Steam ID.
The main device prompt now demonstrates one valid action and separately lists
the enum, reducing the chance that a small model copies a union expression.

Phase 12.4 adds a focused AI window-selection pass. Generic device planning no
longer has final authority over a window ID because its catalog also contains
applications and games. For any requested window operation, a compact AI sees
only currently visible window titles and must choose an exact matching opaque
ID plus the correct bounded action. "Switch to" maps to FOCUS_WINDOW. If no
Steam window is visible, it returns NONE and Casper stops instead of restoring
Edge or silently opening Steam. Python validates the chosen ID against the
same snapshot immediately before applying the fixed Win32 action.

New file:
- prompts/casper_window_action.txt

Phase 12.5 enforces catalog resource types before execution. A small model may
correctly select the Steam window ID but incorrectly label the action as
OPEN_APP. Since kind=window can never be launched, Python treats this only as
a schema type mismatch and hands the original request to Focused Window AI.
That AI still decides FOCUS/MINIMIZE/MAXIMIZE/RESTORE and the exact window ID.
A window resource can no longer reach _launch(), eliminating missing-path
errors without adding keyword-based Python intent judgment.

Phase 12.6 separates device intent family from target selection. Before a
generic OPEN_APP plan can execute while visible windows exist, a compact AI
classifies the requested outcome as OPEN_APPLICATION, WINDOW_CONTROL,
SYSTEM_CONTROL, LIST_LIBRARY, or OTHER. A WINDOW_CONTROL result bypasses the
application catalog entirely and enters Focused Window AI. Thus a request to
switch to Steam cannot launch a different but structurally valid application
such as Code.exe. Python validates the family token and opaque IDs but never
derives intent from user keywords.

New file:
- prompts/casper_device_family.txt

Phase 13.0 begins bounded file and folder actions. AI decides whether the
request means listing, opening, or creating a folder and selects only opaque
IDs from the supplied Desktop/Documents/Downloads/Music/Pictures/Videos
catalog. Python does not infer file intent from keywords; it validates IDs,
root containment, folder-name safety, and performs only the selected action.

Supported Phase 13.0 actions:
- list the immediate contents of one approved user folder
- open one discovered approved user folder, file, or immediate child
- create one direct child folder inside an approved user folder

Not supported yet:
- copy, move, rename, delete, recursive search, arbitrary paths, hidden/system
  locations, shell commands, or editing file contents

New files:
- casper/file_actions.py
- prompts/casper_file_action.txt
- tests/test_file_actions.py

Phase 13.0 validation:
python -m py_compile casper\file_actions.py casper\device_actions.py
python -m unittest -v tests.test_confirmation tests.test_device_actions tests.test_file_actions

Manual tests:
- 下载文件夹里有什么？
- 打开下载文件夹
- 在文档里创建一个报税文件夹

Phase 13.1 fixes a scope collision between local libraries and user folders.
Before LIST_LIBRARY executes, Focused Device Family AI now distinguishes an
installed-game/music-library request from a normal file/folder request. Thus
"下载文件夹里有什么？" enters FILE_ACTION and lists Downloads instead of
silently substituting LOCAL_MUSIC. Python validates the family enum but does
not infer the distinction from Chinese or English keywords.

Phase 13.2 narrows that arbitration to a dedicated two-choice AI decision:
FILE_ACTION or LIST_LIBRARY. The broad device-family classifier can no longer
mislabel a Downloads request as WINDOW_CONTROL and allow the stale music plan
to continue. Invalid AI output is retried once; if still invalid, Bekki asks
which scope the user means instead of returning a false empty-folder result.

New file:
- prompts/casper_file_or_library.txt

Phase 13.3 fixes invalid Melchior enum echo. If the main router copies the
schema union into response_mode, Python detects only that the mode is outside
the allowed enum and asks compact Melchior to judge again. A second invalid
mode raises a routing failure instead of silently falling back to LOCAL_ANSWER
and answering from stale conversation state. Router prompt examples now use
one valid value and list the enum separately.

Phase 13.4 adds Focused Root Selector AI. For LIST_FOLDER and CREATE_FOLDER,
the broad file planner's proposed root is evidence only; a compact AI sees
only the approved user-root names and opaque IDs and selects the requested
root again. This prevents a Downloads request from returning Desktop items.
The final-answer contract must display successful listed_folder items and may
never convert a local file mismatch into a web-search pending action.

New file:
- prompts/casper_file_root.txt

Phase 13.5 removes the broad planner's proposed root ID from Focused Root AI's
input. The proposed ID was anchoring the compact model to the earlier wrong
Desktop selection. Focused Root AI now makes an independent judgment from only
the original user request, recent reference context, and approved root
name-to-opaque-ID catalog. Python still performs no language/alias inference.

Phase 13.6 prevents generative repetition in successful folder listings.
Casper's already validated listed_folder result is rendered directly from its
structured items, exactly once per returned item, instead of asking the long-
form response model to regroup the same list. This renderer makes no semantic
choice; AI still owns intent, action, and root selection. Other responses keep
the normal Bekki generation path.

New file:
- tests/test_adapters.py

Phase 14.0 adds read-only Windows Recycle Bin support. AI distinguishes the
Recycle Bin from ordinary folders and media/game libraries, then selects
LIST_RECYCLE_BIN or OPEN_RECYCLE_BIN. Python uses only fixed Windows Shell
interfaces, caps listing output at 100 items, and directly renders each item
once. Restore, delete, permanent delete, and empty remain unsupported.

New files:
- casper/recycle_bin.py
- prompts/casper_recycle_bin.txt
- tests/test_recycle_bin.py

Manual tests:
- 回收站里有什么？
- 打开回收站
- 清空回收站（must report unsupported）

Phase 14.1 fixes opening the Recycle Bin when the broad planner emits OPEN_APP
with an invented RecycleBin candidate. Before any window selection, a focused
AI gate independently chooses RECYCLE_BIN_ACTION or OTHER. A recycle result
enters the fixed Windows Shell adapter directly, so Bekki's own window cannot
be focused as a substitute. Successful opening also uses a direct authoritative
reply instead of generative reinterpretation.

New file:
- prompts/casper_recycle_scope.txt

Phase 14.2 also applies the focused Recycle Bin gate when a malformed OPEN_APP
plan selects a real window ID. Phase 14.1 covered only invented/missing IDs;
the compact planner could instead select Bekki's valid window and bypass the
gate. Window-typed OPEN_APP targets are now checked for RECYCLE_BIN_ACTION
before Focused Window AI, while valid application targets avoid the extra call.

Phase 15.0 adds supervised restore for one Recycle Bin item. AI selects one
opaque current item ID; Casper displays its name and original location and
pauses. Only an explicit confirmation resumes the original request with the
approved ID. Python rediscovers the Recycle Bin, requires that exact item to
still exist, and invokes only Windows Shell's fixed RESTORE verb. Internal
Recycle Bin paths never reach the model or reply. Empty/permanent deletion
remains unsupported.

New file:
- prompts/casper_recycle_item.txt

Phase 15.1 fixes confirmation-turn reinterpretation. Once the user confirms a
stored restore_recycle_item approval, Casper dispatches that opaque approval
directly to the Recycle Bin executor before generic device planning. The
generic model can no longer rewrite file restore as RESTORE_WINDOW. Successful
window controls also use deterministic structured replies, preventing the
final model from claiming a file was restored when evidence reports only a
focused window.

Phase 15.2 fixes the initial restore request when the generic planner confuses
file restoration with RESTORE_WINDOW. Every bounded window-control plan now
passes through the focused RECYCLE_BIN_ACTION/OTHER gate before window target
selection. A genuine window request continues unchanged; a Recycle Bin item
restore cannot focus VS Code, Edge, or Bekki as a substitute.

Phase 15.3 moves the focused Recycle Bin gate before generic device planning.
Previously a truncated generic JSON response could return early before any
Recycle Bin routing ran. Every device request now gets one compact
RECYCLE_BIN_ACTION/OTHER decision first; recycle requests enter their owning
executor even when the generic planner would truncate, select LIST_LIBRARY,
or choose an unrelated window. Confirmed approvals still bypass all planning.

Phase 15.4 adds a focused restore-intent AI inside the Recycle Bin executor.
Before its broader list/open/unsupported planner runs, AI chooses only
RESTORE_ITEM or NOT_RESTORE. A restore request therefore enters opaque item
selection and confirmation even when the broader recycle planner would have
mistakenly returned LIST_RECYCLE_BIN. Invalid focused output asks for
clarification rather than silently listing.

New file:
- prompts/casper_recycle_restore_intent.txt

Phase 15.5 makes short, explicit device-approval replies deterministic. When
a stored device_action_approval exists, exact replies such as 继续, 确认, 执行,
yes, confirm, continue, and proceed confirm it without calling the generative
confirmation model. This prevents token-limit truncation from turning a
confirmation into a second restore request. The shortcut is unavailable for
ordinary chat and non-device pending actions; ambiguous replies still use AI.

Phase 15.6 fixes the actual Windows Recycle Bin restore invocation. The Shell
API now receives its canonical `undelete` verb instead of the localized-style
`RESTORE` text that Windows could silently ignore while returning exit code 0.
Post-action discovery still verifies that the approved opaque item ID really
left the Recycle Bin before Bekki reports success.

Phase 15.7 adds independent recycle-action arbitration. The focused
restore-intent AI can no longer silently override a broad OPEN_RECYCLE_BIN or
LIST_RECYCLE_BIN judgment because an old chat mentioned restoration. Both AI
judgments are compared; disagreement invokes a separate Gemma 4 arbiter with
CURRENT_REQUEST first and old conversation limited to genuine reference
resolution. Invalid arbitration asks for clarification rather than selecting
an item. Item selection likewise receives the current request before old
context.

New file:
- prompts/casper_recycle_action_arbiter.txt

Phase 16.0 adds the bounded local installation core for Football Manager 2026
tactics. Casper discovers real .fmf candidates inside the user's actual
Downloads locations and discovers existing FM26 data roots under local or
OneDrive Documents. AI selects only opaque source and destination IDs. Python
then validates the selected file, rejects symlinks, unsafe sizes, and disguised
Windows executables, prevents destination escape and silent overwrite, copies
the file, and verifies the installed size before reporting success. No model
may invent a filesystem path. Online tactic research and managed downloading
will connect to this installer in Phase 16.1.

New files:
- casper/game_content.py
- prompts/casper_game_content_install.txt
- tests/test_game_content.py

Phase 16.1 introduces the generic game-content workflow above individual game
adapters. AI distinguishes online research/install requests from already-local
content. For online requests Casper searches public pages, reads candidate and
installation-guide evidence, extracts bounded manifests, and chooses one based
on compatibility, source quality, instruction clarity, and user fit. Manifests
support tactics, mods, saves, plugins, maps, and configuration packs. Source
URLs are copied only from browser evidence; model-supplied or invented URLs and
source IDs are rejected. Destination hints remain logical tutorial evidence,
never assumed local paths. CAPTCHA/access checks pause as browser handoffs.
Phase 16.0 remains only the first FM26 local adapter. Managed downloading,
archive quarantine, and generic deployment will connect in Phase 16.2.

New files:
- casper/content_workflow.py
- casper/content_research.py
- prompts/casper_content_stage.txt
- prompts/casper_content_research_plan.txt
- prompts/casper_content_manifest_extract.txt
- prompts/casper_content_manifest_choose.txt
- tests/test_content_research.py

Phase 16.1.1 fixes upstream routing observed during acceptance testing. A
focused two-token AI gate now runs before the full Melchior router and chooses
CONTENT_DEVICE_ACTION or OTHER. Requests whose final outcome includes game
content installation, downloading, directory placement, importing/enabling, or
explicit installation-manifest preparation enter DEVICE_ACTION before the
general router can confuse tactics/mods with PRODUCT recommendations. Ordinary
"which mod is best" discussion remains recommendation research. Python only
validates the AI token and applies the corresponding bounded route.

New file:
- prompts/melchior_content_action_scope.txt

Phase 16.1.2 fixes the second routing collision found in acceptance testing.
Casper's earliest special-workflow gate now asks AI to choose exactly one of
RECYCLE_BIN_ACTION, GAME_CONTENT_ACTION, or OTHER. A game-content request enters
the generic content workflow before the broad device planner runs and therefore
cannot be mistaken for LIST_RECYCLE_BIN. Recycle Bin behavior remains isolated,
and Python only validates the focused AI token.

Phase 16.1.3 preserves AI decision authority across layers. When Melchior's
focused AI selects CONTENT_DEVICE_ACTION, the normalized plan carries
content_workflow_selected=true into Casper. Casper then bypasses all lower
family gates and invokes the content workflow directly. A second focused AI is
allowed to choose only RESEARCH_AND_INSTALL or INSTALL_LOCAL_CONTENT; it cannot
reclassify the already-owned request as OTHER, Recycle Bin, or an application.
This removes contradictory repeated intent judgments without moving semantic
decisions into Python.

New file:
- prompts/casper_content_authorized_stage.txt

Phase 16.1.4 fixes local-model output exhaustion inside content research. The
compact research-plan and manifest JSON calls now use llama3.2 with thinking
disabled. An empty/invalid research plan gets one compact retry without recent
context. Manifest extraction processes one rendered page per AI call instead
of placing up to eight long pages into a single context, then the existing AI
chooser compares the bounded extracted manifests. This preserves AI ownership
while preventing reasoning text from consuming the entire output budget.

Phase 16.1.5 adds AI-owned search quality control. A focused query reviewer
preserves the full target game/version, selects the language used by the real
content community, and removes ambiguous acronym-only searches. A second AI
ranks bounded Bing results by exact source ID and rejects unrelated companies,
app stores, music/radio pages, cities, and acronym collisions before any page
is read. If no result survives, AI produces one distinct retry query set. The
device adapter also renders Casper's exact clarification directly so the final
reply model cannot invent missing tactic-style or tutorial-format questions.

New files:
- prompts/casper_content_query_review.txt
- prompts/casper_content_source_rank.txt
- prompts/casper_content_query_retry.txt

Phase 16.1.6 separates universal artifact discovery from personal fit. AI now
places a user's team, character, roster, hardware, or play style into
fit_context and comparison criteria instead of automatically adding those names
to web queries. For example, "best tactic suitable for Manchester United"
searches the strongest tested FM26 tactics broadly, then evaluates Manchester
United fit during manifest selection. An entity enters queries only when the
user explicitly requests content created/named for it, such as a tactical
recreation of that team.

Phase 16.1.7 makes that separation fail closed. Every proposed and retry query
must pass a separate focused AI compliance check before browser search begins.
The check recognizes translations, aliases, and abbreviations of fit-context
entities, so "Manchester United" and "曼联" are both rejected from universal
tactic discovery. If query rewriting or compliance checking fails twice, no
old query is used as fallback and no web search is executed.

New file:
- prompts/casper_content_query_compliance.txt

Phase 16.1.8 adds a clean third query rewrite after malformed or noncompliant
AI output. That rewrite sees only the target game/version, content type, and
universal research objective; it cannot echo the original personal-fit entity
because neither the request nor fit_context is included. Structured plan
validation also removes target/version/content-type duplicates accidentally
placed in fit_context, while the independent AI compliance gate still performs
the final semantic decision before any browser search.

Phase 16.1.9 isolates the final compliance judge from the original user
request. It receives only the finished queries, target game/content type,
fit-context exclusions, and an AI-owned boolean indicating whether the user
explicitly requested content created or named for that entity. This prevents a
clean universal query from being rejected merely because the earlier request
mentioned Manchester United, while preserving support for explicit team
recreations.
